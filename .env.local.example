# .env.local.example — Example environment variables for local development
# ------------------------------------------------------------------------------
# Copy this file to `.env.local` and replace the placeholders with your real values.

# --------------------
# SUPABASE (required)
# --------------------
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key   # Optional, server-only (keep secret)

# --------------------
# TTLs (server-side)
# --------------------
# Hours to keep uploaded payment proofs by default (legacy upload TTL)
PAYMENT_PROOF_TTL_HOURS=24
# When marking an order as `delivered`, how long to keep the file (in hours)
DELIVERED_FILE_TTL_HOURS=1
# When marking an order as `delivered`, how long to keep the payment proof (in hours)
DELIVERED_PAYMENT_PROOF_TTL_HOURS=24

# --------------------
# CLIENT / DEMO (public values)
# --------------------
NEXT_PUBLIC_QRIS_URL=/qris-placeholder.svg
# Optional client-side TTL overrides (for demo or local testing)
# NEXT_PUBLIC_PAYMENT_PROOF_TTL_HOURS=24
# NEXT_PUBLIC_DELIVERED_FILE_TTL_HOURS=1
# NEXT_PUBLIC_DELIVERED_PAYMENT_PROOF_TTL_HOURS=24

# --------------------
# ADMIN / DEMO AUTH
# --------------------
# NEXT_PUBLIC_ADMIN_AUTH_TTL_HOURS=24
# NEXT_PUBLIC_DEMO_ADMIN_AUTH_TTL_HOURS=24

# --------------------
# NOTES
# --------------------
# - Do not commit real secrets to git. Use a secrets manager or set the variables in your deployment platform's config.
